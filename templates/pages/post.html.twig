{% extends 'base.html.twig' %}

{% block title %}Hello PagesController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/pa1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .col{
        padding:20px;
        border: solid #eee;
    }
</style>

<div class="example-wrapper container">
    <h1>All Posts✅</h1>
    <div class="row">
    {% for pos in posts %}
        <div class=" col col-md-4">
           <a href="{{path('pos', {id: pos.id})}}"><h3> {{pos.Title}}</h3></a>
           <p> {{pos.texts}}</p>
           <a href="{{path('delete', {id: pos.id})}}">Delete</a>
        </div>
    {% endfor %}
    </div>

</div>
{% endblock %}
